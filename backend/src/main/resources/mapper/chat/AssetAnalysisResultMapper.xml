<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wallo.chat.mapper.AssetAnalysisResultMapper">

    <insert id="insert"
            parameterType="com.wallo.chat.dto.AssetAnalysisResultDto$SaveCommand">
        INSERT INTO ASSET_ANALYSIS_RESULTS (
            user_id,
            assistant_message_id,
            request_message,
            calculated_result,
            ai_response
        ) VALUES (
            #{userId},
            #{assistantMessageId},
            #{requestMessage},
            #{calculatedResultJson},
            #{aiResponse}
        )
    </insert>

    <select id="findByAssistantMessageIds"
            resultType="com.wallo.chat.dto.AssetAnalysisResultDto$StoredResult">
        SELECT assistant_message_id AS assistantMessageId,
               calculated_result AS calculatedResultJson
        FROM ASSET_ANALYSIS_RESULTS
        WHERE assistant_message_id IN
        <foreach collection="assistantMessageIds" item="messageId" open="(" separator="," close=")">
            #{messageId}
        </foreach>
    </select>

</mapper>
