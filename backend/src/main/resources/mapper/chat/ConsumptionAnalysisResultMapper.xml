<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wallo.chat.mapper.ConsumptionAnalysisResultMapper">

    <insert id="insert"
            parameterType="com.wallo.chat.dto.ConsumptionAnalysisResultDto$SaveCommand">
        INSERT INTO CONSUMPTION_ANALYSIS_RESULTS (
            user_id,
            assistant_message_id,
            request_message,
            calculated_result,
            ai_response
        ) VALUES (
            #{userId},
            #{assistantMessageId},
            #{requestMessage},
            #{calculatedResultJson},
            #{aiResponse}
        )
    </insert>

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*)
        FROM CONSUMPTION_ANALYSIS_RESULTS
        WHERE user_id = #{userId}
    </select>

    <select id="findByAssistantMessageIds"
            resultType="com.wallo.chat.dto.ConsumptionAnalysisResultDto$StoredResult">
        SELECT assistant_message_id AS assistantMessageId,
               calculated_result AS calculatedResultJson
        FROM CONSUMPTION_ANALYSIS_RESULTS
        WHERE assistant_message_id IN
        <foreach collection="assistantMessageIds" item="messageId" open="(" separator="," close=")">
            #{messageId}
        </foreach>
    </select>

    <select id="findLatestByUserId"
            resultType="com.wallo.chat.dto.ConsumptionAnalysisResultDto$LatestStoredResult">
        SELECT assistant_message_id AS assistantMessageId,
               request_message AS requestMessage,
               calculated_result AS calculatedResultJson,
               ai_response AS aiResponse,
               generated_at AS generatedAt
        FROM CONSUMPTION_ANALYSIS_RESULTS
        WHERE user_id = #{userId}
        ORDER BY generated_at DESC, assistant_message_id DESC
        LIMIT 1
    </select>

</mapper>
