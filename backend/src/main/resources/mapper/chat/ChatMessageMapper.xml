<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wallo.chat.mapper.ChatMessageMapper">

    <resultMap id="chatMessageResultMap" type="com.wallo.chat.domain.ChatMessage">
        <id property="messageId" column="message_id" />
        <result property="conversationId" column="conversation_id" />
        <result property="role" column="role" />
        <result property="content" column="content" />
        <result property="createdAt" column="created_at" />
    </resultMap>

    <select id="findAllByConversationId" resultMap="chatMessageResultMap">
        SELECT
            message_id,
            conversation_id,
            role,
            content,
            created_at
        FROM CHAT_MESSAGES
        WHERE conversation_id = #{conversationId}
        ORDER BY message_id ASC
    </select>

    <insert id="insert"
            parameterType="com.wallo.chat.domain.ChatMessage"
            useGeneratedKeys="true"
            keyProperty="messageId">
        INSERT INTO CHAT_MESSAGES (
            conversation_id,
            role,
            content,
            created_at
        ) VALUES (
            #{conversationId},
            #{role},
            #{content},
            #{createdAt}
        )
    </insert>
</mapper>
