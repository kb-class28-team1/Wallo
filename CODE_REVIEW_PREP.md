# 소비분석·금융리포트 코드 리뷰 준비 자료

> 범위: 소비분석 에이전트와 금융 뉴스 리포트 에이전트만 다룬다.  
> 사용법: 코드 리뷰 전에 1번을 외우고, 2번 파일을 IDE 탭에 열어 두며, 질문이 나오면 3~4번에서 답을 찾는다.

---

# 1. 전체 흐름을 1분 안에 설명하기

## 1.1 소비분석 에이전트 1분 설명

> 사용자가 채팅에서 소비분석을 요청하면 Spring이 로그인 사용자의 EXPENSE 거래와 예산을 조회해 내부 Python AI 서버에 전달합니다. Python의 고정된 규칙 함수가 기간, 총지출, 증감률, 카테고리 변화, 반복 소비, 정기결제 후보와 예산 상태를 계산하고, AI 모델은 계산된 결과를 바꾸지 않고 설명합니다. Spring은 계산 결과와 AI 답변을 `CONSUMPTION_ANALYSIS_RESULTS`에 저장하고 AI 채팅 메시지와 연결합니다. 덕분에 과거 대화를 다시 열어도 분석 카드를 복원할 수 있고, 저장된 결과는 개인화 미션 생성에도 사용됩니다.

### 20초 축약 버전

> Spring이 로그인 사용자의 거래·예산을 준비하고, Python 코드가 소비 규칙을 계산하며 AI가 이를 설명합니다. 반환된 결과는 Spring이 채팅 메시지와 함께 저장해 카드 복원과 개인화 미션에 사용합니다.

### 흐름 한 줄

`채팅 요청 → Spring 거래·예산 조회 → Python 규칙 계산 → AI 설명 → Spring 결과 저장 → 카드 복원·미션 연결`

## 1.2 금융리포트 에이전트 1분 설명

> 금융리포트 에이전트는 금융 뉴스를 자동 수집하고, AI가 비전문가도 이해할 수 있는 리포트로 바꿔 제공하는 기능입니다.
>
> 먼저 `NewsCrawlingScheduler`가 하루 네 번 실행되고, `NewsCrawler`가 매일경제의 경제·증권·부동산 기사 원문을 수집해 `NEWS` 테이블에 저장합니다. URL에 UNIQUE 제약을 두어 같은 기사는 중복 저장하지 않습니다.
>
> 크롤링 후에는 `FinancialReportGenerationScheduler`가 리포트가 없는 뉴스를 대기열에서 한 건씩 꺼냅니다. `NewsReportGenerationServiceImpl`이 FastAPI를 호출하고, FastAPI는 Groq를 이용해 핵심 요약, 사건 설명, 원인, 사회 영향, 사용자 영향, 대응 전략을 구조화된 JSON으로 생성합니다. Spring은 여섯 영역이 모두 채워졌을 때만 `NEWS_REPORT`에 저장합니다.
>
> 저장 후 금융용어는 AI가 아니라 `FinancialTermMatchingServiceImpl`이 DB 사전을 메모리에 올려 제목과 본문에서 규칙으로 찾고, `NEWS_TERM`에 연결합니다. 화면 목록은 AI 리포트까지 완성된 기사만 보여주며, 오래된 뉴스는 기본 3일 뒤 관련 데이터와 함께 삭제합니다.

### 20초 축약 버전

> 매일경제 뉴스를 수집해 `NEWS`에 저장하고, 백그라운드 워커가 FastAPI/Groq로 여섯 영역의 리포트를 생성해 `NEWS_REPORT`에 저장합니다. 금융용어는 Java가 DB 사전으로 매칭하고, 완성된 기사만 화면에 노출합니다.

### 흐름 한 줄

`뉴스 크롤링 → NEWS → 생성 대기열 → FastAPI/Groq → NEWS_REPORT → 금융용어 매칭 → NEWS_TERM → 목록·상세 화면`

---

# 2. 핵심 파일 5~8개 선정하기

## 2.1 소비분석 에이전트 핵심 8개

1. `ai/app/agents/financial/spending_intent.py`  
   소비 질문 여부와 이번 달·지난달·주간 등 요청 기간을 해석한다.

2. `ai/app/agents/financial/tools/spending_coach.py`  
   총지출, 증감률, 카테고리 변화, 반복 패턴, 정기결제 후보와 예산 상태를 고정 규칙으로 계산한다.

3. `ai/app/agents/financial/consumption_models.py`  
   Spring이 보낸 거래·예산 문맥의 데이터 형식을 검증한다.

4. `backend/src/main/java/com/wallo/asset/service/ConsumptionAnalysisContextService.java`  
   로그인 사용자의 EXPENSE 거래와 예산을 조회해 Python에 보낼 문맥을 만든다.

5. `backend/src/main/java/com/wallo/chat/service/ChatService.java`  
   재사용할 최신 분석을 확인하고 Python AI 서버 호출을 연결한다.

6. `backend/src/main/java/com/wallo/chat/service/ConsumptionAnalysisResultService.java`  
   계산 결과 JSON과 AI 답변을 저장하고 과거 분석을 조회한다.

7. `backend/src/main/resources/mapper/chat/ConsumptionAnalysisResultMapper.xml`  
   `CONSUMPTION_ANALYSIS_RESULTS`의 INSERT와 조회 SQL을 정의한다.

8. `frontend/src/components/analysis/AnalysisResult.vue`  
   저장·반환된 소비분석 결과를 채팅 카드로 표시한다.

## 2.2 금융리포트 에이전트 핵심 8개

### 1) `NewsCrawlingScheduler.java`

- 리뷰 포인트: 언제 수집하고 다음 작업을 어떻게 시작하는가
- 보여줄 내용: KST 06·12·15·18시, 중복 실행 방지, 생성 요청 연결
- 한 문장: “뉴스 수집 전체 흐름의 시작점입니다.”
- 경로: `backend/src/main/java/com/wallo/report/scheduler/NewsCrawlingScheduler.java`

### 2) `NewsCrawler.java`

- 리뷰 포인트: 외부 뉴스에서 어떤 데이터를 어떻게 가져오는가
- 보여줄 내용: 세 섹션, CSS 선택자, 본문 정제, URL 중복 확인
- 한 문장: “매일경제 목록과 상세 페이지를 읽어 정제된 원문 뉴스를 만듭니다.”
- 경로: `backend/src/main/java/com/wallo/report/crawler/NewsCrawler.java`

### 3) `FinancialReportGenerationScheduler.java`

- 리뷰 포인트: AI 작업을 크롤링과 분리한 이유와 대기열 처리
- 보여줄 내용: 한 건씩 처리, 실행 잠금, 실패 ID 격리, 수동 요청
- 한 문장: “리포트 없는 기사를 안전하게 순차 처리하는 백그라운드 워커입니다.”
- 경로: `backend/src/main/java/com/wallo/report/scheduler/FinancialReportGenerationScheduler.java`

### 4) `NewsReportGenerationServiceImpl.java`

- 리뷰 포인트: 금융리포트 생성의 핵심 업무 흐름
- 보여줄 내용: 기존 결과 재사용, 빈 본문 차단, AI 검증, 동시 INSERT 충돌, 용어 매칭
- 한 문장: “뉴스 한 건을 완성된 리포트와 용어 연결로 바꾸는 중심 Service입니다.”
- 경로: `backend/src/main/java/com/wallo/report/service/NewsReportGenerationServiceImpl.java`

### 5) `PythonNewsReportAiClient.java`

- 리뷰 포인트: Spring과 FastAPI의 경계
- 보여줄 내용: 요청 JSON, 타임아웃, HTTP/파싱 오류 변환
- 한 문장: “Java 업무 코드가 AI 구현 세부사항에 직접 의존하지 않게 하는 통신 어댑터입니다.”
- 경로: `backend/src/main/java/com/wallo/report/client/PythonNewsReportAiClient.java`

### 6) `ai/app/reports/router.py`

- 리뷰 포인트: AI 출력 형식과 검증
- 보여줄 내용: JSON Schema, 길이 제한, 재시도, 목업 모드
- 한 문장: “Groq가 반드시 정해진 여섯 영역을 반환하도록 통제하는 AI API입니다.”
- 경로: `ai/app/reports/router.py`

### 7) `FinancialTermMatchingServiceImpl.java`

- 리뷰 포인트: AI와 분리된 금융용어 매칭
- 보여줄 내용: 전체 사전 캐시, 문자열 정규화, 경계 검사, 재매칭 저장
- 한 문장: “뉴스 본문에서 금융용어를 규칙으로 찾아 DB 관계를 만드는 서비스입니다.”
- 경로: `backend/src/main/java/com/wallo/report/term/service/FinancialTermMatchingServiceImpl.java`

### 8) `NewsMapper.xml`

- 리뷰 포인트: 목록 노출 기준, 생성 대상, 보관기간 삭제
- 보여줄 내용: `INNER JOIN NEWS_REPORT`, 미생성 대상 조회, 삭제 순서용 SQL
- 한 문장: “어떤 뉴스가 생성 대상이고 어떤 뉴스가 사용자에게 보이는지를 결정합니다.”
- 경로: `backend/src/main/resources/mapper/report/NewsMapper.xml`

보조로 열어 둘 파일:

- `ai/app/reports/prompts.py`
- `backend/src/main/resources/mapper/report/NewsReportMapper.xml`
- `backend/src/main/resources/mapper/report/NewsTermMapper.xml`
- `database/sql/news.sql`, `news_report.sql`, `financial_term.sql`, `news_term.sql`

---

# 3. 중요한 ‘왜’와 답변 준비하기

## 3.1 소비분석의 중요한 ‘왜’

### 왜 Python이 소비 금액과 비율을 계산하나요?

AI 모델이 계산하는 것이 아니라 `spending_coach.py`의 일반 함수가 정해진 공식과 임계값으로 계산한다. 같은 입력에는 같은 결과가 나오고 Python 단위 테스트로 경계를 검증할 수 있다. 소비 규칙과 AI 도구 실행을 한 서버에서 빠르게 연결하기 위해 이 구조를 선택했다.

### Spring은 왜 필요한가요?

Spring은 로그인 사용자 확인, 거래·예산 조회, 채팅 메시지 저장, 분석 결과 저장, 과거 카드 복원과 미션 연결을 담당한다. Python은 DB에 직접 접근하지 않는다.

### 왜 계산 결과를 JSON으로 저장하나요?

카테고리, 패턴, 신호처럼 구조가 중첩된 결과를 채팅 메시지와 함께 그대로 복원하기 위해서다. `assistant_message_id`로 해당 AI 메시지와 연결한다.

### 왜 이전 분석을 재사용하나요?

같은 소비 질문마다 동일한 데이터를 다시 계산하고 AI를 재호출하는 비용을 줄이기 위해 최신 결과를 재사용할 수 있다. 재사용 여부는 응답의 `consumptionAnalysisReused`로 전달된다.

### 왜 이전 기간 지출이 0원이면 증감률을 만들지 않나요?

0으로 나눌 수 없고 임의의 퍼센트는 의미를 왜곡한다. 신규 소비 상태로 구분하고 현재 금액을 설명한다.

### 왜 원본 거래를 AI 모델이 임의로 해석하게 두지 않나요?

Python 규칙 함수가 먼저 금액과 패턴을 확정하고, AI 프롬프트에는 계산 결과를 다시 계산하거나 변경하지 말라는 제약을 둔다. 숫자 판정과 자연어 설명의 책임을 분리하기 위해서다.

## 3.2 금융리포트의 중요한 ‘왜’

### 왜 크롤링과 AI 생성을 분리했나요?

AI 호출은 느리거나 실패할 수 있다. 두 작업을 묶으면 AI 장애 때문에 수집한 뉴스까지 저장되지 않을 수 있으므로, 원문 저장을 먼저 끝내고 AI 생성은 별도 작업으로 처리한다.

### 왜 외부 Tomcat/Spring 트랜잭션으로 AI 호출 전체를 묶지 않나요?

AI 호출은 최대 수십 초 걸릴 수 있어 그동안 DB 연결과 트랜잭션을 잡고 있으면 자원이 낭비된다. 실제 저장은 단일 INSERT이고 DB UNIQUE 제약으로 정합성을 보장한다.

### 왜 뉴스 URL에 UNIQUE 제약을 두나요?

스케줄이 하루 여러 번 실행되고 목록에 같은 기사가 반복 노출될 수 있기 때문이다. 애플리케이션의 사전 검사와 DB UNIQUE를 함께 사용한다.

### 왜 리포트는 뉴스당 한 건인가요?

현재 화면은 기사 하나에 대표 분석 하나를 보여주는 구조다. `NEWS_REPORT.news_id UNIQUE`로 중복 생성 경쟁도 막는다.

### 왜 AI 응답 여섯 영역이 모두 있어야 저장하나요?

일부 필드만 있는 반쪽짜리 리포트가 목록에 노출되면 사용자가 상세 화면에서 빈 섹션을 보게 된다. 완전한 응답만 저장하고 실패한 기사는 다음 작업에서 다시 시도한다.

### 왜 요약 bullet을 별도 테이블이 아니라 줄바꿈 TEXT로 저장하나요?

현재 요약은 2~5개의 짧은 문장이고 별도 검색·정렬 요구가 없어 한 리포트의 한 필드로 취급했다. 다만 bullet 개별 편집·순서 관리가 필요해지면 별도 테이블이나 JSON이 더 적합하다.

### 왜 AI 출력에 JSON Schema를 사용하나요?

자연어만 요청하면 필드 누락이나 형식 변화가 잦다. Spring이 예측 가능한 구조를 받도록 이름, 타입, 필수 필드, 길이를 제한한다.

### 왜 JSON 검증 실패 시 한 번만 재시도하나요?

일시적인 형식 오류는 회복하되, 무한 재시도로 비용과 지연이 커지는 것을 막기 위해서다. 한 번이라는 수치의 제품적 근거는 코드상 확인되지 않으며 운영 지표로 조정할 수 있다.

### 왜 기사 본문을 앞 4000자와 뒤 1000자로 자르나요?

모델 입력 길이와 비용을 제한하면서, 주요 사실이 많은 기사 앞부분과 결론·전망이 나오는 뒷부분을 함께 보존하려는 절충이다.

### 왜 리포트 목록은 `NEWS_REPORT`가 있는 기사만 보여주나요?

사용자가 카드를 눌렀는데 “아직 생성 중”인 빈 상세를 보지 않게 하기 위해서다. 생성 완료가 노출 조건이다.

### 왜 금융용어 매칭을 AI가 아니라 Java 규칙으로 하나요?

사전에 있는 용어만 일관되게 연결하고, 같은 기사에 같은 결과를 재현하며, AI 비용과 장애에 의존하지 않기 위해서다.

### 왜 금융용어 전체를 메모리에 캐시하나요?

기사마다 사전 전체를 DB에서 읽으면 반복 비용이 크다. 사전은 자주 바뀌지 않으므로 한 번 컴파일해 재사용하고 기본 1시간마다 갱신한다.

### 왜 용어 재매칭 때 기존 `NEWS_TERM`을 지우고 다시 넣나요?

사전이나 매칭 규칙이 바뀌었을 때 오래된 연결이 남지 않게 현재 결과로 완전히 교체하기 위해서다. 복합 PK로 중복도 방지한다.

### 왜 용어 매칭 실패가 리포트 생성 전체를 실패시키지 않나요?

용어 설명은 부가 기능이고 핵심 리포트는 이미 완성됐기 때문이다. 부가 기능 오류 때문에 사용자가 리포트 자체를 못 보는 것보다 로그를 남기고 리포트를 제공하는 것이 낫다고 판단한 구조다.

### 왜 생성 작업을 한 번에 한 건씩 처리하나요?

동시 AI 호출로 API 제한과 서버 부하가 급증하는 것을 막기 위해서다. 처리량은 낮아지지만 안정성과 비용 예측이 쉬워진다.

### 왜 실패 뉴스 ID를 같은 실행에서 다시 시도하지 않나요?

항상 실패하는 기사 하나가 대기열 첫 자리를 계속 차지해 다른 기사가 처리되지 않는 현상을 막기 위해서다.

### 왜 오래된 뉴스는 기본 3일 뒤 삭제하나요?

서비스가 최신 금융 뉴스 중심이고 크롤링 원문과 AI 결과가 지속해서 쌓이는 것을 막기 위해서다. 정확한 3일의 제품 정책 근거는 코드상 확인되지 않으므로 운영 요구에 따라 설정해야 한다.

### 왜 삭제 순서가 `NEWS_TERM → NEWS_REPORT → NEWS`인가요?

두 자식 테이블이 NEWS를 외래키로 참조한다. 부모부터 지우면 외래키 오류가 발생하므로 연결과 리포트를 먼저 삭제한다.

### 왜 금융용어 캐시 갱신 실패 시 기존 캐시를 유지하나요?

일시적인 DB 장애 때문에 용어 기능 전체가 빈 상태가 되는 것을 막기 위해서다. 최신성은 잠시 늦어져도 기존 용어 매칭은 계속할 수 있다.

### 왜 FastAPI를 별도 서버로 분리했나요?

Java 업무 처리와 Python AI SDK·프롬프트·모델 호출을 분리하면 모델 변경과 AI 실험이 Spring 배포 구조에 덜 영향을 준다.

### 왜 `NewsReportAiClient` 인터페이스가 필요한가요?

Service가 Python HTTP 구현에 직접 묶이지 않게 하고, 테스트에서는 가짜 Client를 넣으며, 추후 공급자를 바꿀 수 있게 하기 위해서다.

---

# 4. 모든 경계 상황과 예상 답변

“모든”은 코드에서 확인 가능한 분기와 오류 조건을 기준으로 정리한다.

## 4.1 소비분석 계산 경계

| 상황 | 현재 처리 |
|---|---|
| 소비 질문이 아님 | 일반 금융 채팅 경로 사용 |
| 기간 표현 없음 | 기본 소비분석 기간 사용 |
| 이전 기간 지출 0원 | 증감률을 억지로 만들지 않고 신규 소비로 처리 |
| 거래 부족 | 패턴을 단정하지 않고 데이터 부족 안내 |
| 예산 미설정 | 예산 신호 없이 분석 계속 |
| 단발성 고액 거래 | 반복 습관과 구분해 감축 제안을 제한 |
| 최근 완료 주 부족 | 반복 패턴 조건을 만족하지 않으면 패턴 미생성 |
| AI 설명 실패 | 오류 처리 정책에 따라 요청 실패 또는 규칙 결과 보존 여부를 확인해야 함 |
| 최신 분석 재사용 가능 | DB 결과를 재사용해 Python 재호출 생략 |
| 후속 소비 질문 | 저장된 최근 분석 기간을 문맥으로 전달 |

## 4.6 소비 채팅 경계

| 상황 | 현재 처리/확인 사항 |
|---|---|
| 소비 질문이 아닌 일반 금융 질문 | 다른 답변 또는 다른 도구 경로 |
| “지난번보다 어때?” 같은 후속 질문 | 대화 문맥과 후속 표현으로 소비 의도 판단 |
| 사용자가 기간을 명시 | 해당 기간 인자를 우선 구성 |
| 기간을 명시하지 않음 | 기본 기간 규칙 적용 |
| 거래가 적거나 없음 | 데이터 부족 구조를 반환하고 단정적 코칭을 피함 |
| 예산이 없음 | 예산 비교 없이 거래 기반 분석 |
| AI 호출 제한 | FastAPI가 rate-limit 오류를 명시적 응답으로 변환 |
| AI가 잘못된 도구 인자 생성 | JSON 파싱/모델 검증 단계에서 방어 |

## 4.7 금융 뉴스 수집 경계

| 상황 | 현재 처리 |
|---|---|
| 같은 URL이 이미 DB에 있음 | 저장 건너뜀, DB UNIQUE가 최종 방어 |
| 목록 페이지 로딩 실패 | 해당 크롤링 실행 오류 로그 |
| 기사 상세 하나 실패 | 구현상 기사별 오류 격리 여부는 `NewsCrawler` 흐름에 따라 로그/스킵; 리뷰 시 해당 catch 위치를 함께 확인 |
| 제목·본문·URL 등 필수값 누락 | `NewsService` 저장 검증 실패 |
| 게시일 파싱 실패 | 크롤러의 fallback 여부를 코드에서 확인; 임의 추측 금지 |
| 사이트 CSS 구조 변경 | 기사를 못 찾을 수 있음. 선택자 기반 크롤러의 구조적 위험 |
| 스케줄과 수동 크롤링 동시 실행 | `AtomicBoolean`로 새 실행 거절 |
| 섹션에 기사 3개 초과 | 섹션당 최대 3개만 처리 |

## 4.8 금융 AI 생성 경계

| 상황 | 현재 처리 |
|---|---|
| 뉴스 ID가 없음 | `REPORT_NOT_FOUND`, 스케줄에서는 스킵 |
| 본문이 null/공백 | `REPORT_CONTENT_EMPTY`, 생성 대상 SQL에서도 제외 |
| 기존 리포트 존재 | AI 재호출 없이 기존 결과 반환, 용어만 재매칭 |
| AI 서버 연결 실패 | AI 서버 오류로 변환, 리포트 저장 안 함 |
| AI HTTP 오류 | 프로젝트 오류로 변환, 저장 안 함 |
| AI 응답 JSON 오류 | FastAPI에서 한 번 재시도; 최종 실패 시 저장 안 함 |
| 요약 목록이 비어 있음 | Spring 최종 검증 실패 |
| 요약 중 빈 문자열이 있음 | Spring 최종 검증 실패 |
| 사건·원인·영향·전략 중 하나가 빔 | Spring 최종 검증 실패 |
| 동시에 같은 뉴스 생성 | `news_id UNIQUE`; 늦은 INSERT는 충돌 후 기존 행 재조회 |
| 자동 생성 설정 false | 일반 자동 작업은 실행하지 않음. 수동 요청/대기열 경로의 세부 동작은 메서드별로 구분 |
| 같은 실행에서 특정 뉴스 반복 실패 | 실패 ID 집합으로 다시 선택하지 않음 |
| 처리할 뉴스가 없음 | 요청 상태 종료, 빈 대기열 로그 |

## 4.9 금융용어 경계

| 상황 | 현재 처리 |
|---|---|
| 사전이 비어 있음 | 매칭 0건; 리포트 자체는 제공 |
| 같은 용어가 본문에 여러 번 있음 | NEWS_TERM에는 한 번만 연결 |
| 용어가 제목과 본문 모두에 있음 | 한 번만 연결 |
| 짧고 흔한 문자열 | 최소 길이와 경계 규칙으로 오탐 방지 |
| 용어 캐시 갱신 실패 | 기존 캐시 유지 |
| 서버 실행 후 사전 변경 | 기본 1시간 이내 캐시 갱신 |
| 재매칭 결과가 달라짐 | 기존 NEWS_TERM 삭제 후 현재 결과 저장 |
| 용어 매칭 중 예외 | 로그만 남기고 NEWS_REPORT 생성은 성공 유지 |
| `short_definition` 없음 | 프론트가 전체 정의 등 fallback을 사용 |

## 4.10 금융리포트 화면·보관 경계

| 상황 | 현재 처리 |
|---|---|
| NEWS만 있고 NEWS_REPORT 없음 | 목록에 표시하지 않음 |
| 상세 URL에 없는 newsId 입력 | 리포트 없음 오류/준비 상태 처리 |
| 읽지 않은 리포트 | 새 글 상태 표시 |
| 읽은 리포트 | 브라우저 localStorage 기준 읽음 표시 |
| 다른 브라우저/기기 | 읽음 상태 공유 안 됨 |
| 게시일 기준 보관기간 초과 | 매일 00:10 삭제 |
| 삭제 시 외래키 관계 존재 | NEWS_TERM, NEWS_REPORT, NEWS 순서로 삭제 |
| `retentionDays < 1` 설정 | 서버 구성 오류로 차단 |

---

# 5. 테스트와 미완성 범위를 명확하게 밝히기

## 5.1 소비분석 테스트 설명

### 현재 공식 경로에서 확인할 테스트

- Python 기간 해석과 소비 질문 판별
- Python 총지출·증감·카테고리·예산·반복 패턴 계산
- Spring 거래·예산 문맥 생성
- Python AI 요청·응답 연결
- `CONSUMPTION_ANALYSIS_RESULTS` 저장과 최신 결과 재사용
- 과거 채팅의 소비분석 카드 복원
- 저장 결과를 이용한 개인화 미션 생성
- 프론트 소비분석 카드 렌더링

### 명확히 밝힐 범위

> 소비분석의 공식 계산 엔진은 Python `spending_coach.py`다. 과거에 별도로 만들었던 Java `com.wallo.spending` 계산 모듈과 미사용 `SPENDING_ANALYSES` 설계는 제거했으며, 공식 저장 테이블은 `CONSUMPTION_ANALYSIS_RESULTS`다.

### 코드 리뷰 시 먼저 제안할 개선점

- 숫자가 포함된 “3달 전” 같은 기간 표현 지원 범위를 명확히 한다.
- Python에 전달하는 원본 거래 범위와 개인정보 최소화 정책을 검토한다.
- 계산 결과 JSON에 스키마·분석 로직 버전을 두는 방안을 검토한다.
- Python 장애 시 규칙 결과를 어느 수준까지 보존할지 명확히 한다.
- 경계값과 분석 문구가 테스트로 함께 고정됐는지 확인한다.

## 5.2 금융리포트 테스트 설명

### 테스트가 보장하는 것

| 테스트 | 보장 내용 |
|---|---|
| `ReportControllerTest.java` | 목록·상세·생성·수동 작업 API 응답 |
| `NewsServiceImplTest.java` | 뉴스 입력 검증, 중복, 목록·상세 조합 |
| `NewsReportGenerationServiceImplTest.java` | 기존 결과 재사용, 빈 AI 필드 차단, INSERT 충돌, 용어 매칭 격리 |
| `FinancialTermMatchingServiceImplTest.java` | 문자열 경계, 정규화, 오탐 방지, NEWS_TERM 저장 |
| `NewsCrawlingSchedulerTest.java` | 스케줄 실행과 중복 실행 방지, 생성 요청 |
| `FinancialReportGenerationSchedulerTest.java` | 배치 대상, 우선순위, 성공·실패·스킵, 잠금 |
| `NewsRetentionSchedulerTest.java` | 보관 기준일 계산과 삭제 호출 |
| `FinancialTermCacheRefreshSchedulerTest.java` | 캐시 주기 갱신과 예외 격리 |
| `MatchedTermResponseTest.java` | 용어 응답 변환 |
| `ai/tests/test_financial_report.py` | FastAPI 리포트 요청, 목업/실제 호출 경계, 출력 검증 |

### 리뷰에서 보여주기 좋은 테스트 세 개

1. AI 응답 여섯 영역 중 하나가 비면 DB에 저장하지 않는 테스트
2. 동시에 같은 뉴스가 저장될 때 UNIQUE 충돌 후 기존 리포트를 반환하는 테스트
3. 용어 매칭 실패가 리포트 생성 자체를 실패시키지 않는 테스트

### 금융리포트 미완성·제한 범위

반드시 밝힐 내용:

> 뉴스 수집, AI 생성, DB 저장, 용어 매칭, 목록·상세 화면까지 주요 흐름은 연결돼 있습니다. 다만 사용자 영향 분석은 실제 로그인 사용자 자산이 아니라 FastAPI의 고정 데모 프로필 ID 7을 사용합니다. 크롤링과 생성용 수동 API의 관리자 권한도 확인되지 않아 운영 전 보완이 필요합니다.

구체적인 제한:

- `userImpact`가 실제 로그인 사용자별 결과가 아님
- 매일경제 HTML/CSS 구조 변경 시 크롤러가 깨질 수 있음
- 수동 크롤링·생성·테스트 API에 관리자 권한 확인 안 됨
- AI 실패 뉴스의 장기 재시도·재시도 횟수·dead-letter 정책 없음
- 기존 리포트를 진짜로 재생성/UPDATE하는 기능 없음
- 프론트/API 주석의 “재생성·교체”와 실제 `generateIfAbsent` 동작 불일치
- 읽음 상태가 서버가 아닌 브라우저 localStorage에만 존재
- 기본 3일 보관 정책의 제품 근거 확인 필요
- AI 품질을 자동 평가하는 테스트는 코드상 확인되지 않음
- 실제 MySQL·Tomcat·FastAPI·Groq·Selenium을 모두 포함한 전체 E2E 자동 테스트는 코드상 확인되지 않음

### 금융리포트에서 코드 리뷰 시 먼저 제안할 개선점

- 실제 사용자 자산 문맥을 Spring에서 FastAPI로 전달한다.
- 수동/테스트 API를 제거하거나 관리자 권한으로 제한한다.
- “재생성” API라면 UPDATE/versioning을 구현하고, 아니라면 이름과 주석을 “기존 결과 보장”으로 수정한다.
- 크롤러 선택자 실패 모니터링과 fixture 기반 파싱 테스트를 추가한다.
- 실패 횟수·다음 재시도 시각·최종 실패 상태를 DB로 관리한다.
- 보관기간과 목록 기간 조건을 하나의 설정값으로 통일한다.

---

# 코드 리뷰 당일 진행 대본

## 시작

> 제가 리뷰받을 기능은 소비분석 에이전트와 금융 뉴스 리포트 에이전트 두 가지입니다. 먼저 각 기능의 전체 흐름을 설명하고, 핵심 파일 위주로 설계 이유와 경계값, 테스트, 아직 연결되지 않은 범위를 말씀드리겠습니다.

## 소비분석 시작

> 소비분석은 Python의 고정된 규칙 함수가 금액·비율·패턴을 계산하고 AI 모델이 그 결과를 설명합니다. Spring은 로그인 사용자의 거래·예산 조회, 채팅 연결, 결과 저장과 카드 복원을 담당합니다.

그다음 2.1의 파일을 순서대로 연다.

## 금융리포트 시작

> 금융리포트는 크롤링 실패와 AI 실패가 서로 영향을 주지 않도록 원문 수집과 리포트 생성을 분리했습니다. AI 응답은 구조와 필수값을 이중 검증하고, 금융용어는 AI가 아니라 DB 사전 기반으로 매칭합니다.

그다음 2.2의 파일을 순서대로 연다.

## 미완성 범위 고지

> 소비분석의 공식 계산 경로는 Python이고 결과는 CONSUMPTION_ANALYSIS_RESULTS에 저장됩니다. 금융리포트의 주요 흐름은 연결됐지만 사용자 영향은 데모 프로필 기반이고 운영용 권한과 재시도 정책은 보완이 필요합니다.

## 리뷰 요청

> 특히 Python 소비 규칙의 테스트·버전 관리와 금융리포트 실패 재시도, 실제 사용자 개인화를 어떤 우선순위로 가져갈지 의견을 받고 싶습니다.

---

# 최종 체크리스트

- [ ] 소비분석 1분 설명을 소리 내어 한 번 읽었다.
- [ ] 금융리포트 1분 설명을 소리 내어 한 번 읽었다.
- [ ] 핵심 파일 8개씩 IDE 탭에 열어 두었다.
- [ ] 거래 부족, 예산 미설정, 이전 0원과 반복 패턴 경계를 설명할 수 있다.
- [ ] 이번 달·지난달·주간·후속 질문의 기간 해석을 설명할 수 있다.
- [ ] 뉴스 중복, AI 빈 응답, 동시 생성, 용어 실패를 설명할 수 있다.
- [ ] 금융리포트 사용자 영향이 고정 데모 프로필임을 먼저 밝힌다.
- [ ] 테스트 세 개씩 바로 열 수 있다.
- [ ] 피드백을 버그·필수 개선·제안·질문으로 구분해 기록한다.


