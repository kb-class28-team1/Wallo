OPENAI_API_KEY=your-api-key
OPENAI_MODEL=gpt-4o-mini
# 비워두면 위 OPENAI_MODEL을 그대로 재사용한다. 리포트 생성만 다른 모델을 쓰고 싶을 때만 설정.
OPENAI_REPORT_MODEL=
# true일 때만 /api/reports/generate가 [MOCK] 더미 응답을 반환한다. 기본값은 false(실제 OpenAI 호출).
AI_REPORT_MOCK_ENABLED=false
