GROQ_API_KEY=your-groq-api-key
GROQ_MODEL=openai/gpt-oss-20b
# 비워두면 위 GROQ_MODEL을 그대로 재사용한다. 리포트 생성만 다른 모델을 쓰고 싶을 때만 설정.
GROQ_REPORT_MODEL=
# true일 때만 /api/reports/generate가 [MOCK] 더미 응답을 반환한다. 기본값은 false(실제 Groq 호출).
AI_REPORT_MOCK_ENABLED=false
